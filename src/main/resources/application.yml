server:
  port: 8080
spring:
    datasource:
        url: jdbc:mariadb://localhost:3306/fitness_management
        username: root
        password: zooin.net
        driver-class-name: org.mariadb.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: update
        database-platform:
        properties:
            hibernate:
                show_sql: true
                format_sql: true

logging:
  level:
    org:
      springframework:
        security: TRACE
