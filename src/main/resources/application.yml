server:
  port: 8080
spring:
    datasource:
        url: jdbc:h2:mem:test
        username: sa
        password:
        driver-class-name: org.h2.Driver
    h2:
      console:
        enabled: true
        path: /h2-console
    jpa:
        hibernate:
#            ddl-auto: create
        database-platform:
        properties:
            hibernate:
                show_sql: true
                format_sql: true
